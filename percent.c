#include"main.h"
#include<stdio.h>
/**
 * choose - choose the function and do the process
 * @c: is a char
 * Return: 0 (success)
 */
int choose(char c)
{
	int count = 0;

	_putchar(c);
	count += 1;
	return (count);
}
