#include <stdio.h>
#include "main.h"
/**
 * elsee - print the text
 * @count: is the number of letters
 * @t: is the letter
 * Return: 0 (success)
 */
int elsee(int count, char t)
{
	_putchar(t);
	count += 1;
	return (count);
}
